apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.config.name }}
  labels:
    {{- include "django-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config.environmentVariables }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}